/**
 * top-kek-rpg-client/src/types/index.tsx
 * Stores the interface for the types to be used in the redux state management
 */

import * as alias from './alias';

/********************************** STORE TYPES ********************************/

export interface StoreState {
  testState: alias.Value;
}